'use strict';

describe('Factory', function() {
  var Factory = require('../src');
  var Finder  = require('../src/finder');
  var construct = Factory;
  var cb;

  describe('new Factory(options): Finder', function() {

  });
});
